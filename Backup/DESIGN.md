# DESIGN.md — TeeHidZ Dashboard

> เอกสารระบบดีไซน์ของหน้า "หน้าหลัก" (Dashboard) ของ TeeHidZ (ระบบจัดการห้องเช่า/คอนโด และอสังหาฯ ให้เช่า)
> ภาพอ้างอิง: `assets/reference-dashboard.png` (1536 × 1024 px)
>
> ⚠️ ค่าสี ฟอนต์ และขนาด ถอดจากภาพหน้าจอด้วยการประมาณ — ถ้ามีไฟล์ Figma ต้นฉบับ ให้ยึดตาม Figma เป็นหลัก

---

## 1. ภาพรวม

| หัวข้อ | รายละเอียด |
|---|---|
| ชื่อผลิตภัณฑ์ | **TeeHidZ** — "จัดการห้องเช่า/คอนโด...ให้ง่ายขึ้น" |
| ผู้ใช้หลัก | เจ้าของห้องเช่า / เจ้าของคอนโด / ผู้จัดการโครงการ (ภาษาไทย) |
| งานหลักของหน้านี้ | ดูภาพรวมห้องเช่า/คอนโดในหน้าเดียว: ห้อง ผู้เช่า รายได้ งานแจ้งซ่อม และทางลัดไปงานที่ทำบ่อย |
| อารมณ์ของดีไซน์ | สะอาด เป็นมิตร น่าเชื่อถือ โทนเขียวธรรมชาติ ให้ความรู้สึก "บ้าน" มากกว่า "ระบบบัญชี" |
| โหมดสี | Light mode เท่านั้น (v1) |
| ภาษา UI | ไทยทั้งหมด ปีเป็น พ.ศ. |

---

## 2. Design Tokens

### 2.1 สี (Color)

#### Brand / Surface

| Token | Hex (ประมาณ) | ใช้ที่ |
|---|---|---|
| `--color-sidebar-from` | `#0F4F3C` | Sidebar ไล่สีด้านบน |
| `--color-sidebar-to` | `#0A3A2C` | Sidebar ไล่สีด้านล่าง |
| `--color-sidebar-active` | `#1F7A55` | พื้นหลังเมนูที่เลือกอยู่ |
| `--color-sidebar-text` | `#FFFFFF` | ข้อความ/ไอคอนเมนู |
| `--color-sidebar-card` | `#15634A` | การ์ดข้อมูลโครงการ (เช่น "สุขสันต์ คอนโด") ใน sidebar |
| `--color-primary` | `#1E8A5A` | ปุ่มหลัก, แท่งรายรับ, donut ส่วนมีผู้เช่า, ลิงก์ "ดูทั้งหมด" |
| `--color-primary-hover` | `#177049` | hover ของปุ่มหลัก |
| `--color-primary-soft` | `#E8F5EE` | พื้นหลังการ์ด KPI ห้อง, badge "มีผู้เช่า", ปุ่มรอง |
| `--color-bg` | `#F4F7F6` | พื้นหลังของ main area |
| `--color-surface` | `#FFFFFF` | การ์ดทั้งหมด |
| `--color-border` | `#E6ECEA` | เส้นขอบการ์ด / เส้นคั่นแถว |

#### Text

| Token | Hex | ใช้ที่ |
|---|---|---|
| `--color-text` | `#1C2B26` | หัวข้อ, ตัวเลขหลัก |
| `--color-text-secondary` | `#5B6B66` | ข้อความรอง, คำอธิบาย |
| `--color-text-muted` | `#8A9793` | เวลา, label แกนกราฟ, ข้อความเล็ก |

#### Semantic / Accent (ใช้กับการ์ด KPI, ไอคอนแจ้งเตือน, เมนูด่วน)

| Token | Solid | Soft (พื้นหลัง) | ความหมาย |
|---|---|---|---|
| `green` | `#22A06B` | `#EAF6EF` | ห้องพัก, ชำระเงินแล้ว, สำเร็จ |
| `blue` | `#3B82F6` | `#EEF4FE` | ผู้เช่า, สัญญา, สถานะ "ว่าง" |
| `orange` | `#F59E0B` | `#FFF6E8` | รายได้, ห้องว่างพร้อมให้เช่า, สร้างสัญญา |
| `red` | `#EF4444` | `#FDEEEE` | แจ้งซ่อม, รายจ่าย, ต้องดำเนินการ |
| `purple` | `#8B5CF6` | `#F3EEFE` | ปุ่มลัด "แจ้งซ่อม" |
| `teal` | `#14B8A6` | `#E7F8F6` | ปุ่มลัด "ดูรายงาน" |
| `gray` | `#6B7280` | `#F1F3F4` | ปุ่มลัด "ตั้งค่า" |

#### Chart

| Token | Hex | ใช้ที่ |
|---|---|---|
| `--chart-income` | `#1E8A5A` (ไล่ลงเป็น `#2FA872`) | แท่งรายรับ |
| `--chart-expense` | `#B9CCDD` (ไล่ลงเป็น `#DCE6EF`) | แท่งรายจ่าย |
| `--chart-occupied` | `#1E8A5A` | Donut: มีผู้เช่า |
| `--chart-vacant` | `#C9DCE8` | Donut: ว่าง |
| `--chart-grid` | `#EEF1F0` | เส้นกริดแนวนอน |

> กฎ: ห้ามใช้สีดิบ (hex) ในคอมโพเนนต์ ให้อ้างผ่าน token เสมอ

### 2.2 ตัวอักษร (Typography)

| บทบาท | ฟอนต์ | หมายเหตุ |
|---|---|---|
| หัวข้อ + ตัวเลข | **Prompt** (Google Fonts) | ฟอร์มกลมมน มีหัว เข้ากับภาพ |
| เนื้อหา | **Prompt** น้ำหนัก 400 หรือ **IBM Plex Sans Thai** | ถ้าต้องการอ่านง่ายขึ้นในตาราง |
| Tagline ลายมือใน sidebar | **Mali** หรือ **Itim** | ใช้ที่เดียวเท่านั้น |

Fallback stack: `"Prompt", "IBM Plex Sans Thai", "Noto Sans Thai", system-ui, sans-serif`

| Style | Size / Line-height | Weight | ตัวอย่าง |
|---|---|---|---|
| `display` | 28 / 36 | 600 | สวัสดีครับ คุณเจ้าของ/ผู้ดูแล 👋 |
| `kpi-value` | 26 / 32 | 600 | 48 ห้อง, ฿ 126,500 |
| `stat-value` | 20 / 28 | 600 | ฿ 52,300 |
| `h2` (หัวการ์ด) | 18 / 26 | 600 | รายรับ - รายจ่าย (เดือนนี้) |
| `body` | 15 / 24 | 400 | ตรวจสอบข้อมูลห้องเช่า/คอนโด... |
| `body-strong` | 14 / 20 | 500 | ห้อง 101, ชื่อผู้เช่า |
| `caption` | 12 / 18 | 400 | ชั้น 1 \| 3,500 บาท/เดือน |
| `micro` | 11 / 16 | 400 | 1 ชม.ที่แล้ว, label แกนกราฟ |

- หน่วยที่ต่อท้ายตัวเลข KPI (ห้อง, คน, รายการ) ใช้ขนาด 16px น้ำหนัก 500
- ตัวเลขใช้ `font-variant-numeric: tabular-nums` ในตารางและกราฟ

### 2.3 ระยะห่าง (Spacing) — ฐาน 4px

`4, 8, 12, 16, 20, 24, 32, 40`

- ระยะห่างระหว่างการ์ด (gap): **20px**
- Padding ภายในการ์ด: **20px** (การ์ดใหญ่ 24px)
- Padding ของ main area: **20px**

### 2.4 มุมโค้ง (Radius)

| Token | ค่า | ใช้ที่ |
|---|---|---|
| `--radius-sm` | 8px | badge, thumbnail ห้อง |
| `--radius-md` | 12px | ไอคอนสี่เหลี่ยม, ปุ่ม, เมนู sidebar, การ์ดย่อยสรุปยอด |
| `--radius-lg` | 16px | การ์ดหลักทั้งหมด |
| `--radius-full` | 9999px | avatar, badge แจ้งเตือน, pill สถานะ |

### 2.5 เงา (Shadow)

| Token | ค่า |
|---|---|
| `--shadow-card` | `0 1px 2px rgba(16, 40, 32, 0.04), 0 4px 16px rgba(16, 40, 32, 0.04)` |
| `--shadow-hover` | `0 6px 20px rgba(16, 40, 32, 0.08)` |

เงาบางมาก การ์ดแยกจากพื้นหลังด้วยเส้นขอบ `--color-border` เป็นหลัก

---

## 3. Layout

### 3.1 โครงหน้า (Desktop ≥ 1280px)

```
┌──────────┬─────────────────────────────────────────────┬──────────────────┐
│          │                                              │ 🔔 💬 [Profile ▾] │  ← Topbar (อยู่ในคอลัมน์ขวา)
│ SIDEBAR  │  [ HERO BANNER .......................  🏢 ] ├──────────────────┤
│ 252px    │                                              │  แจ้งเตือน        │
│          │  [KPI ห้อง][KPI ผู้เช่า][KPI รายได้][KPI ซ่อม] │  (4 รายการ)       │
│ โลโก้     │                                              ├──────────────────┤
│ เมนู 9   │  [ รายรับ-รายจ่าย (กราฟ+สรุป) ][ สถานะห้อง ]  │  เมนูด่วน 3×2    │
│          │                                              ├──────────────────┤
│ การ์ดที่พัก│  [ ห้องพักล่าสุด         ][ ชำระค่าเช่าล่าสุด ] │  Promo card      │
│ tagline  │                                              │                  │
└──────────┴─────────────────────────────────────────────┴──────────────────┘
```

| ส่วน | ความกว้าง |
|---|---|
| Sidebar | `252px` fixed, สูงเต็มจอ, `position: sticky` |
| Main (คอลัมน์กลาง) | `minmax(0, 1fr)` |
| Right rail | `340px` |
| Gap ระหว่างคอลัมน์ | `20px` |

แถวในคอลัมน์กลาง:

1. Hero banner — สูงประมาณ 140px
2. KPI 4 ใบ — `grid-template-columns: repeat(4, 1fr)` สูงประมาณ 125px
3. กราฟ (≈ 58%) + สถานะห้อง (≈ 42%) — `grid-template-columns: 1.4fr 1fr`
4. ห้องพักล่าสุด + การชำระค่าเช่าล่าสุด — `grid-template-columns: 1.4fr 1fr`

การจัดแนว: ข้อความชิดซ้ายทั้งหมด ตัวเลขในการ์ดสรุปชิดซ้าย เปอร์เซ็นต์ชิดขวา เวลาในรายการแจ้งเตือนชิดขวาบน

### 3.2 Responsive

| Breakpoint | พฤติกรรม |
|---|---|
| `≥ 1280px` | 3 คอลัมน์ตามภาพ |
| `1024–1279px` | Right rail ย้ายลงไปใต้คอลัมน์กลาง แสดงเป็น 3 การ์ดเรียงแนวนอน (แจ้งเตือน / เมนูด่วน / promo) |
| `768–1023px` | Sidebar ย่อเหลือไอคอน (72px) มี tooltip; KPI เป็น 2×2; แถวกราฟและตารางเรียงเป็น 1 คอลัมน์ |
| `< 768px` | Sidebar กลายเป็น drawer (ปุ่ม ☰ ที่ topbar); KPI 2×2; ภาพตึกใน hero ซ่อน; ตารางห้องพักเปลี่ยนเป็นการ์ดรายการ; ซ่อน tagline ลายมือ |

ห้าม body เลื่อนแนวนอน เนื้อหากว้าง (ตาราง) ให้ `overflow-x: auto` ภายในการ์ดของตัวเอง

---

## 4. Components

### 4.1 Sidebar

- พื้นหลัง: linear-gradient จาก `--color-sidebar-from` ไป `--color-sidebar-to`
- **โลโก้**: ไอคอนบ้านสีขาว (40px) + "TeeHidZ" (22px / 600) + tagline "จัดการห้องเช่า/คอนโด...ให้ง่ายขึ้น" (13px, หรือปรับตามประเภทอสังหาฯ ที่ตั้งค่าไว้จากหลังบ้าน)
- **เมนู** (ไอคอน 22px + ข้อความ 15px, ความสูงแถว 48px, radius 12px, margin-x 14px):

| # | ข้อความ | ไอคอน (lucide) | Route |
|---|---|---|---|
| 1 | หน้าหลัก | `Home` | `/` |
| 2 | ห้องพัก | `BedDouble` | `/rooms` |
| 3 | ผู้เช่า | `Users` | `/tenants` |
| 4 | สัญญาเช่า | `FileText` | `/leases` |
| 5 | การเงิน | `CircleDollarSign` | `/finance` |
| 6 | แจ้งซ่อม / บำรุงรักษา | `Wrench` | `/maintenance` |
| 7 | รายงาน | `FileBarChart` | `/reports` |
| 8 | ข้อความ | `MessageSquareMore` | `/messages` |
| 9 | ตั้งค่า | `Settings` | `/settings` |

  - สถานะ active: พื้นหลัง `--color-sidebar-active`, ข้อความ 500
  - hover: พื้นหลังขาวโปร่ง 8%
  - ชื่อไอคอน lucide อาจต่างกันตามเวอร์ชัน ให้เลือกตัวที่ใกล้เคียงที่สุด

- **การ์ดโครงการ/ที่พัก** (ล่างเมนู): ไอคอนบ้านในวงกลมขาว + "สุขสันต์ คอนโด" (14px/600) + "ห้องพัก 48 ห้อง | ผู้เช่า 42 คน" (11px) + pill "● Premium" (พื้นเขียวอ่อน) — ชื่อและประเภทปรับได้จากหลังบ้าน
- **Tagline ลายมือ**: "บริหารห้องเช่าและคอนโด ให้เป็นเรื่องง่าย ด้วยเทคโนโลยี" (หรือปรับตามประเภทอสังหาฯ) เอียงประมาณ -8° สีขาว + ภาพภูเขา/ต้นไม้สีเขียวเข้มที่ขอบล่าง (SVG ตกแต่ง)

### 4.2 Topbar (มุมขวาบน)

- ปุ่มไอคอน `Bell` มี badge แดงตัวเลข (เช่น `3`) — ขนาด badge 16px
- ปุ่มไอคอน `MessageSquare`
- การ์ดโปรไฟล์: avatar วงกลม 44px + ชื่อ (14px/500) + บทบาท "เจ้าของ/ผู้จัดการ" (12px muted) + `ChevronDown`
- ปุ่มไอคอนทุกปุ่มต้องมี `aria-label` ภาษาไทย

### 4.3 Hero Banner

- การ์ดสีขาว radius 16px, ภาพตึกห้องเช่า/คอนโดชิดขวา (ประมาณ 40% ของความกว้าง) มี gradient ขาว→โปร่งใส ทางซ้ายของภาพเพื่อให้กลืนกับพื้น
- เนื้อหา:
  - หัวข้อ `display`: "สวัสดีครับ คุณ{ชื่อเล่น/บทบาท} 👋"
  - คำอธิบาย `body`: "ตรวจสอบข้อมูลห้องเช่า/คอนโด และจัดการทุกอย่างได้ในที่เดียว" (ข้อความปรับตามประเภทที่ตั้งค่าไว้จากหลังบ้าน)
  - แถววันที่: ไอคอน `Calendar` + "วันนี้ **11 กันยายน 2568**" + "เวลา 20:12 น." (วันที่และเวลาเป็นค่าจริงแบบ dynamic)

### 4.4 KPI Card (× 4)

โครงสร้าง:

```
┌───────────────────────────────┐
│ [icon 48px]  ชื่อ KPI        >  │
│              ค่า  หน่วย         │
│ ───────────────────────────── │
│ ข้อความรอง | ข้อความรอง         │
└───────────────────────────────┘
```

- พื้นหลังการ์ด = สี soft ของ tone, ไอคอนอยู่ในสี่เหลี่ยม radius 12px สี solid ไอคอนขาว
- ทั้งการ์ดคลิกได้ (มี `ChevronRight` สื่อว่าไปหน้ารายละเอียด)

| Tone | ไอคอน | ชื่อ | ค่า | บรรทัดล่าง | ลิงก์ |
|---|---|---|---|---|---|
| green | `Home` | ห้องทั้งหมด | 48 ห้อง | ว่าง 6 ห้อง \| มีผู้เช่า 42 ห้อง | `/rooms` |
| blue | `Users` | ผู้เช่าทั้งหมด | 42 คน | ชาย 18 คน \| หญิง 24 คน | `/tenants` |
| orange | `CircleDollarSign` | รายได้เดือนนี้ | ฿ 126,500 | ↑ 12% จากเดือนที่แล้ว (สีเขียว) | `/finance` |
| red | `AlertTriangle` | แจ้งซ่อมค้าง | 3 รายการ | กำลังดำเนินการ 2 \| รอรับ 1 | `/maintenance` |

- ตัวบ่งชี้เปลี่ยนแปลง: ↑ สีเขียวเมื่อผลดี, ↓ สีแดงเมื่อผลเสีย (ดูกฎใน RULES.md)

### 4.5 การ์ด "รายรับ - รายจ่าย"

- หัวการ์ด + legend ขวาบน: ● รายรับ (เขียว) ● รายจ่าย (เทาฟ้า)
- ซ้าย (≈ 60%): **Grouped bar chart**
  - แกน X: 3 เดือนล่าสุด (ก.ค., ส.ค., ก.ย.) ใช้ชื่อเดือนย่อภาษาไทย
  - แกน Y: 0 – 200,000 ขั้นละ 50,000 จัดรูปแบบมีคอมมา
  - แท่งกว้างประมาณ 22px, radius ด้านบน 4px, มี gradient แนวตั้ง
  - Tooltip เมื่อ hover: "ก.ย. 2568 · รายรับ ฿126,500"
- ขวา (≈ 40%): การ์ดย่อยสรุป 3 ใบเรียงแนวตั้ง radius 12px

| ชื่อ | ค่า | เปลี่ยนแปลง | พื้นหลัง |
|---|---|---|---|
| รายรับรวม | ฿ 126,500 | ↑ 12% (เขียว) | `green.soft` |
| รายจ่ายรวม | ฿ 52,300 | ↑ 5% (แดง) | `red.soft` |
| กำไรสุทธิ | ฿ 74,200 | ↑ 18% (เขียว) | `green.soft` |

### 4.6 การ์ด "สถานะห้องพัก"

- **Donut chart** เส้นผ่านศูนย์กลางประมาณ 140px ความหนาวง 22px
  - กลางวง: "ทั้งหมด" (12px) + "48 ห้อง" (20px/600)
  - ส่วน: มีผู้เช่า (`--chart-occupied`), ว่าง (`--chart-vacant`)
- Legend ขวา: "● มีผู้เช่า 42 (88%)", "● ว่าง 6 (12%)"
- ปุ่มรองขวาล่าง: "ดูรายละเอียดห้องพัก ›" — พื้น `--color-primary-soft` ข้อความ `--color-primary` radius 12px

### 4.7 ตาราง "ห้องพักล่าสุด"

- หัวการ์ด + ลิงก์ "ดูทั้งหมด" สีเขียว (ขวา)
- แถวละ 1 ห้อง สูงประมาณ 58px เส้นคั่นบาง ๆ
- คอลัมน์: `[thumbnail 56×40 radius 8]` `[ห้อง / ชั้น | ราคา]` `[ผู้เช่า / หมดสัญญา]` `[badge สถานะ]` `[ChevronRight]`
- ห้องว่าง: ชื่อผู้เช่าแสดง "-" และไม่มีบรรทัดหมดสัญญา
- Badge สถานะ (pill, 12px/500, padding 4×12):

| สถานะ | ข้อความ | พื้นหลัง | ข้อความ |
|---|---|---|---|
| `OCCUPIED` | มีผู้เช่า | `green.soft` | `#1E8A5A` |
| `VACANT` | ว่าง | `blue.soft` | `#3B82F6` |

- ทั้งแถวคลิกได้ → `/rooms/{id}`, hover พื้นหลัง `#F7FAF9`

### 4.8 รายการ "การชำระค่าเช่าล่าสุด"

- แถว: `[avatar 36px]` `[ชื่อผู้เช่า / ห้อง | จำนวนเงิน / วันที่ เวลา]` `[badge สถานะ]`
- Badge `PAID` = "ชำระแล้ว" (สไตล์เดียวกับ "มีผู้เช่า")
- สถานะอื่นที่ต้องรองรับ: `PENDING` = "รอชำระ" (orange.soft), `OVERDUE` = "เกินกำหนด" (red.soft)
- ไม่มีรูปผู้เช่า → avatar ตัวอักษรตัวแรกของชื่อ (ตัดคำนำหน้า น.ส./นาย/นาง ออก) บนพื้นสีจาก tone

### 4.9 การ์ด "แจ้งเตือน"

- หัวการ์ด + "ดูทั้งหมด"
- แถว: `[ไอคอนวงกลม 40px สี solid]` `[หัวข้อ 14px/600 / รายละเอียด 12px]` `[เวลาแบบสัมพัทธ์ 11px ขวาบน]`

| ประเภท | ไอคอน | สี |
|---|---|---|
| `MAINTENANCE_NEW` | `Wrench` | red |
| `PAYMENT_RECEIVED` | `CircleDollarSign` | green |
| `LEASE_EXPIRING` | `FileText` | blue |
| `ROOM_AVAILABLE` | `AlertTriangle` | orange |

- รายการที่ยังไม่อ่าน: ข้อความหัวข้อ 600 (อ่านแล้ว 500) — ไม่ใช้จุดสีเพิ่ม

### 4.10 การ์ด "เมนูด่วน"

- Grid 3 × 2, ปุ่มแต่ละปุ่มเป็นการ์ดขาวขอบบาง radius 12px สูงประมาณ 88px
- ไอคอนอยู่ในสี่เหลี่ยม 36px radius 10px สี solid + ข้อความ 12px ด้านล่าง

| ข้อความ | ไอคอน | สี | ปลายทาง |
|---|---|---|---|
| เพิ่มผู้เช่า | `UserPlus` | green | `/tenants/new` |
| บันทึกค่าเช่า | `ReceiptText` | blue | `/finance/payments/new` |
| แจ้งซ่อม | `Wrench` | purple | `/maintenance/new` |
| สร้างสัญญาเช่า | `FilePlus` | orange | `/leases/new` |
| ดูรายงาน | `FileBarChart` | teal | `/reports` |
| ตั้งค่า | `Settings` | gray | `/settings` |

### 4.11 Promo Card

- พื้นหลังไล่สีเขียวอ่อนมาก (`#EEF7F2` → `#FFFFFF`) + ภาพตึกชิดขวาล่าง
- ไอคอนบ้านเขียว, หัวข้อ 2 บรรทัด "ห้องเช่าและคอนโดของคุณ / จัดการได้ง่ายขึ้น" (20px/600)
- pill "ด้วย TeeHidZ" (เขียวอ่อน)
- ปุ่มหลัก "ดูวิธีใช้งาน ›" (พื้น `--color-primary` ข้อความขาว radius 12px) → `/help`
- ผู้ใช้ปิดการ์ดนี้ได้ (ปุ่ม X ขนาดเล็ก แสดงตอน hover) และจำค่าไว้

### 4.12 Dialog & Alert (SweetAlert2)

- ใช้ `sweetalert2` ร่วมกับ `sweetalert2-react-content` ในการแสดง Modal แจ้งเตือนและ Confirm Dialog
- **สไตล์ตาม Design System**:
  - ฟอนต์: `Prompt, sans-serif`
  - ขอบมน (border-radius): `16px` สำหรับ popup card, `10px` สำหรับปุ่ม
  - ปุ่มยืนยัน (Confirm Button): สี `--color-primary` (`#1E8A5A`), hover `#177049`, ข้อความสีขาว
  - ปุ่มยกเลิก (Cancel Button): สีเทาอ่อน ขอบ `--color-border` (`#E6ECEA`), ข้อความ `--color-text-secondary`
  - ไอคอนสถานะ: success (เขียวระบบ), warning (ส้ม `--color-warning`), error (แดง `--color-danger`)
  - Toast Notification: แสดงมุมขวาบน (`top-end`), auto-close 3000ms, พร้อม timer progress bar

---

## 5. สถานะของ UI

| สถานะ | แนวทาง |
|---|---|
| Loading | Skeleton ขนาดเท่าเนื้อหาจริง สี `#EEF2F0` ห้ามใช้ spinner กลางการ์ด |
| Empty | ข้อความบอกสิ่งที่ทำได้ต่อ เช่น "ยังไม่มีการชำระค่าเช่าเดือนนี้" + ปุ่ม "บันทึกค่าเช่า" |
| Error | "โหลดข้อมูลรายรับไม่สำเร็จ" + ปุ่ม "ลองอีกครั้ง" อยู่ในการ์ดนั้น ๆ ไม่ทำให้ทั้งหน้าพัง |
| Hover | การ์ดที่คลิกได้: เงา `--shadow-hover` + เลื่อนขึ้น 1px |
| Focus | `outline: 2px solid #1E8A5A; outline-offset: 2px` มองเห็นได้ทุกจุดที่กดได้ |
| Disabled | opacity 0.5, cursor not-allowed |

## 6. Motion

- จุดเคลื่อนไหวหลักจุดเดียว: ตอนโหลดหน้า แท่งกราฟและ donut วาดขึ้นใน 600ms (`ease-out`)
- hover / press: 150ms
- เคารพ `prefers-reduced-motion: reduce` → ปิดแอนิเมชันกราฟทั้งหมด
- ห้ามใส่ fade-in ทุก section

## 7. รูปภาพ

| ภาพ | สัดส่วน | ไฟล์แนะนำ |
|---|---|---|
| Hero ตึกห้องเช่า/คอนโด | ประมาณ 16:6 ครอปชิดขวา | `public/images/hero-building.webp` |
| Promo ตึก | ประมาณ 1:1.3 | `public/images/promo-building.webp` |
| Thumbnail ห้อง | 7:5 | จาก `Room.coverImageUrl` หรือ placeholder |
| Avatar | 1:1 | จาก `Tenant.avatarUrl` หรือ initials |

- ใส่ `alt` ภาษาไทยที่บอกเนื้อหา เช่น "อาคารสุขสันต์ คอนโด / ห้องเช่า"
- ภาพตกแต่งล้วน (ภูเขาใน sidebar) ใช้ `alt=""` และ `aria-hidden="true"`

## 8. Tailwind config (ตัวอย่าง)

```ts
// tailwind theme extension (ใช้กับ Tailwind v4 ผ่าน @theme ได้เช่นกัน)
colors: {
  sidebar: { from: "#0F4F3C", to: "#0A3A2C", active: "#1F7A55", card: "#15634A" },
  primary: { DEFAULT: "#1E8A5A", hover: "#177049", soft: "#E8F5EE" },
  bg: "#F4F7F6",
  surface: "#FFFFFF",
  line: "#E6ECEA",
  ink: { DEFAULT: "#1C2B26", secondary: "#5B6B66", muted: "#8A9793" },
  tone: {
    green:  { solid: "#22A06B", soft: "#EAF6EF" },
    blue:   { solid: "#3B82F6", soft: "#EEF4FE" },
    orange: { solid: "#F59E0B", soft: "#FFF6E8" },
    red:    { solid: "#EF4444", soft: "#FDEEEE" },
    purple: { solid: "#8B5CF6", soft: "#F3EEFE" },
    teal:   { solid: "#14B8A6", soft: "#E7F8F6" },
    gray:   { solid: "#6B7280", soft: "#F1F3F4" },
  },
},
borderRadius: { sm: "8px", md: "12px", lg: "16px" },
fontFamily: {
  sans: ["Prompt", "IBM Plex Sans Thai", "Noto Sans Thai", "system-ui", "sans-serif"],
  hand: ["Mali", "Itim", "cursive"],
},
```
